pipeline {
    agent any
    stages {
        stage('init') {
            steps {
                echo "Init stage"
                echo git --version
            }
        }
        stage('build') {
            steps {
                echo "Buid stage"
                echo git --version
            }
        }
        stage('deploy') {
            steps {
                echo "Deploy stage"
                echo git --version
            }
        }

    }
}